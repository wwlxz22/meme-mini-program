<scroll-view style='width: 100%; height: 100%;'>
  <view class='classi-area flex-row items-center'>
    <block wx:for="{{classificationList}}" wx:for-item="item" wx:key="key">
      <classi-item class="classi-item" cid="{{item.category_id}}" cover="{{item.cover}}" title="{{item.name}}">{{item.name}}</classi-item>
    </block>
  </view>
</scroll-view>